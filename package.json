{
  "name": "spatial-grid-2d",
  "version": "1.0.0",
  "description": "A spatial partitioning 2D grid for efficient many-to-many collision detection and such.",
  "main": "dist/SpatialGrid.js",
  "types": "dist/SpatialGrid.d.ts",
  "files": [
    "dist/"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/spatial-grid.git"
  },
  "keywords": [
    "spatial-grid",
    "collision-detection",
    "2d-grid",
    "spatial-partitioning"
  ],
  "bugs": {
    "url": "https://github.com/aaronbeall/spatial-grid/issues"
  },
  "homepage": "https://github.com/aaronbeall/spatial-grid#readme",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "esbuild src/* example/* --bundle --outdir=docs --format=esm --servedir=docs --watch",
    "build:docs": "esbuild src/* example/* --bundle --outdir=docs --format=esm --minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.12",
    "lit-html": "^3.2.1",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.3"
  }
}
